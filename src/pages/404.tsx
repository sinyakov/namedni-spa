import React from "react";

import Layout from "../components/Layout/Layout";
import SEO from "../components/seo";

const NotFoundPage = () => (
  <Layout>
    <SEO title="Ничего не найдено" />
    <h1>Ничего не найдено</h1>
  </Layout>
);

export default NotFoundPage;
